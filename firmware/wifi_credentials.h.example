#pragma once
// Copy this file to wifi_credentials.h and fill in your home network.
// wifi_credentials.h is gitignored so credentials never get committed.

#define NETWORK_SSID "your-wifi-ssid"
#define NETWORK_PASS "your-wifi-password"
